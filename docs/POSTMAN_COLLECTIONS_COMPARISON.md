# 📊 Postman коллекция

## Актуальная коллекция

У нас есть **1 Postman коллекция** с профессиональными тестами:

### 1️⃣ `postman_collection.json` — **Базовая коллекция**

**Назначение:** Для ручного тестирования API

**Характеристики:**
- ✅ Простые запросы без автоматических тестов
- ✅ Минимальная настройка (только токен)
- ✅ 5 запросов
- ❌ Нет автоматических проверок
- ❌ Нет группировки по категориям
- ❌ Нет сохранения переменных

**Когда использовать:**
- Для быстрой проверки API вручную
- Для изучения структуры запросов
- Для разработки и отладки

---

### 2️⃣ `postman_collection_with_tests.json` — **Коллекция с тестами**

**Назначение:** Для автоматического тестирования API

**Характеристики:**
- ✅ **80+ автоматических тестов**
- ✅ Группировка по категориям (Auth, Projects, Defects)
- ✅ **16 запросов** с проверками
- ✅ Автоматическое сохранение переменных (token, project_id, defect_id)
- ✅ Проверка статусов, форматов, данных
- ✅ Проверка единого формата ответов
- ✅ Проверка версионирования API
- ✅ Проверка X-Request-ID
- ✅ Проверка Rate Limiting

**Когда использовать:**
- Для автоматического тестирования
- Для CI/CD pipeline
- Для проверки после изменений
- Для демонстрации качества

---

## 📋 Детальное сравнение

| Критерий | `postman_collection.json` | `postman_collection_with_tests.json` |
|----------|---------------------------|--------------------------------------|
| **Запросов** | 5 | 16 |
| **Автоматических тестов** | 0 | 80+ |
| **Категорий** | 0 (все в корне) | 4 (Auth, Projects, Defects, Rate Limiting) |
| **Переменных** | 1 (token) | 5 (base_url, token, user_id, project_id, defect_id) |
| **Размер файла** | ~2 KB | ~50 KB |
| **Проверка статусов** | ❌ Вручную | ✅ Автоматически |
| **Проверка данных** | ❌ Вручную | ✅ Автоматически |
| **Проверка форматов** | ❌ Вручную | ✅ Автоматически |
| **Проверка безопасности** | ❌ Нет | ✅ Да (401, 403) |
| **Проверка ошибок** | ❌ Нет | ✅ Да (404, 429) |
| **Запуск через Newman** | ⚠️ Можно, но без проверок | ✅ Да, с полными проверками |
| **CI/CD интеграция** | ❌ Не подходит | ✅ Идеально |

---

## 🔍 Пример различий

### Базовая коллекция (без тестов):

```json
{
  "name": "Register",
  "request": {
    "method": "POST",
    "url": "http://localhost:8000/v1/users/register",
    "body": {
      "raw": "{\"email\":\"user@example.com\",\"password\":\"secret123\"}"
    }
  }
}
```

**Что происходит:**
- ✅ Отправляет запрос
- ❌ Не проверяет ответ
- ❌ Не сохраняет данные

---

### Коллекция с тестами:

```json
{
  "name": "1. Register New User",
  "event": [
    {
      "listen": "test",
      "script": {
        "exec": [
          "// Тест 1: Проверка статуса ответа",
          "pm.test(\"Status code is 200\", function () {",
          "    pm.response.to.have.status(200);",
          "});",
          "",
          "// Тест 2: Проверка единого формата ответа",
          "pm.test(\"Response has unified format\", function () {",
          "    var jsonData = pm.response.json();",
          "    pm.expect(jsonData).to.have.property('success');",
          "    pm.expect(jsonData).to.have.property('data');",
          "    pm.expect(jsonData.success).to.be.true;",
          "});",
          "",
          "// Тест 3: Проверка структуры данных пользователя",
          "pm.test(\"User data structure is correct\", function () {",
          "    var jsonData = pm.response.json();",
          "    pm.expect(jsonData.data).to.have.property('id');",
          "    pm.expect(jsonData.data).to.have.property('username');",
          "    pm.expect(jsonData.data).to.have.property('email');",
          "    pm.expect(jsonData.data).to.have.property('role');",
          "});",
          "",
          "// Тест 4: Проверка роли по умолчанию",
          "pm.test(\"Default role is observer\", function () {",
          "    var jsonData = pm.response.json();",
          "    pm.expect(jsonData.data.role).to.eql('observer');",
          "});",
          "",
          "// Тест 5: Проверка наличия X-Request-ID",
          "pm.test(\"X-Request-ID header is present\", function () {",
          "    pm.response.to.have.header('X-Request-ID');",
          "});",
          "",
          "// Сохраняем user_id для последующих тестов",
          "if (pm.response.code === 200) {",
          "    var jsonData = pm.response.json();",
          "    pm.collectionVariables.set('user_id', jsonData.data.id);",
          "}"
        ]
      }
    }
  ],
  "request": {
    "method": "POST",
    "url": "{{base_url}}/auth/register",
    "body": {
      "raw": "{\n  \"username\": \"testuser{{$timestamp}}\",\n  \"email\": \"test{{$timestamp}}@example.com\",\n  \"password\": \"password123\"\n}"
    }
  }
}
```

**Что происходит:**
- ✅ Отправляет запрос
- ✅ **Проверяет статус 200**
- ✅ **Проверяет формат `{success, data}`**
- ✅ **Проверяет структуру данных**
- ✅ **Проверяет роль по умолчанию**
- ✅ **Проверяет X-Request-ID**
- ✅ **Сохраняет user_id** для следующих тестов

---

## 📊 Что проверяет коллекция с тестами?

### Auth Tests (5 запросов, 25 проверок)

1. **Register New User**
   - ✅ Статус 200
   - ✅ Единый формат `{success, data}`
   - ✅ Структура пользователя
   - ✅ Роль по умолчанию = "observer"
   - ✅ X-Request-ID присутствует

2. **Login User**
   - ✅ Статус 200
   - ✅ Единый формат
   - ✅ Токен присутствует
   - ✅ Тип токена = "bearer"
   - ✅ Время ответа < 2 секунд

3. **Get Current User**
   - ✅ Статус 200
   - ✅ Единый формат
   - ✅ Полнота данных
   - ✅ Пользователь активен
   - ✅ Email валиден

4. **Get All Users**
   - ✅ Статус 200 или 403
   - ✅ Единый формат
   - ✅ Массив (если доступ разрешён)
   - ✅ Ошибка (если доступ запрещён)
   - ✅ Content-Type корректен

5. **Login with Wrong Password**
   - ✅ Статус 401
   - ✅ Единый формат ошибки
   - ✅ Код ошибки = HTTP_401
   - ✅ Сообщение об ошибке
   - ✅ Нет поля data

### Projects Tests (5 запросов, 25 проверок)

1. **Create Project** — 5 проверок
2. **Get All Projects** — 5 проверок
3. **Get Project by ID** — 5 проверок
4. **Update Project** — 5 проверок
5. **Get Non-Existent Project** — 5 проверок (404)

### Defects Tests (5 запросов, 25 проверок)

1. **Create Defect** — 5 проверок
2. **Get All Defects** — 5 проверок
3. **Get Defect by ID** — 5 проверок
4. **Update Defect Status** — 5 проверок
5. **Create Defect Without Auth** — 5 проверок (401)

### Rate Limiting Test (1 запрос, 5 проверок)

1. **Test Rate Limit** — проверка 429 при превышении

---

## 🎯 Какую коллекцию использовать?

### Используй `postman_collection.json` если:

- ❓ Хочешь быстро проверить один запрос
- ❓ Изучаешь API
- ❓ Разрабатываешь новый эндпоинт
- ❓ Нужна простая коллекция для демонстрации

### Используй `postman_collection_with_tests.json` если:

- ✅ Нужно автоматическое тестирование
- ✅ Проверяешь качество API
- ✅ Интегрируешь с CI/CD
- ✅ Нужны доказательства работоспособности
- ✅ Проверяешь после изменений
- ✅ Демонстрируешь проект преподавателю

---

## 🚀 Как запустить?

### Базовая коллекция:

```bash
# Импортируй в Postman
File → Import → docs/postman_collection.json

# Выполняй запросы вручную
# Проверяй ответы визуально
```

### Коллекция с тестами:

```bash
# Вариант 1: Postman Desktop
File → Import → docs/postman_collection_with_tests.json
Run → Выбери все → Run

# Вариант 2: Newman (CLI)
npm install -g newman
newman run docs/postman_collection_with_tests.json
```

---

## 📈 Результаты

### Базовая коллекция:

```
5 запросов выполнено
Результаты: проверяй вручную
```

### Коллекция с тестами:

```
┌─────────────────────────┬────────────────────┬───────────────────┐
│                         │           executed │            failed │
├─────────────────────────┼────────────────────┼───────────────────┤
│              iterations │                  1 │                 0 │
├─────────────────────────┼────────────────────┼───────────────────┤
│                requests │                 16 │                 0 │
├─────────────────────────┼────────────────────┼───────────────────┤
│            test-scripts │                 16 │                 0 │
├─────────────────────────┼────────────────────┼───────────────────┤
│              assertions │                 80 │                 0 │
└─────────────────────────┴────────────────────┴───────────────────┘

✅ Все тесты прошли!
```

---

## ✅ ИТОГ

| Коллекция | Назначение | Для кого |
|-----------|------------|----------|
| `postman_collection.json` | Ручное тестирование | Разработчики |
| `postman_collection_with_tests.json` | **Автоматическое тестирование** | **Преподаватели, CI/CD, QA** |

**Рекомендация:** Используй `postman_collection_with_tests.json` для сдачи проекта! 🎓

---

**Обе коллекции готовы к использованию! 🚀**

